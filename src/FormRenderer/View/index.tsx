import React from 'react';
import topContext from '../context/top';
import { IFormRendererProps, IFormViewProps } from '../type';

export default (props: IFormRendererProps) => {
  const { data } = props;
};
